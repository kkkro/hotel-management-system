@model DatPhong

@{
    Layout = "_DashboardLayout";
    ViewData["Title"] = "Chinh Sua Dat Phong";
}

<style>
    :root {
        --primary: #00236f;
        --primary-container: #1e3a8a;
        --surface: #f8f9ff;
        --surface-container: #eff4ff;
        --on-surface: #0d1c2e;
    }

    .edit-container {
        background: white;
        border-radius: 1.25rem;
        padding: 2rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        max-width: 800px;
        margin: 0 auto;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-label {
        font-weight: 600;
        color: var(--on-surface);
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 0.5rem;
    }

    .form-control, .form-select {
        border: 1px solid #c5c5d3;
        border-radius: 0.5rem;
        padding: 0.75rem 1rem;
        font-family: 'Inter', sans-serif;
    }

    .form-control:focus, .form-select:focus {
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(0, 35, 111, 0.1);
    }

    .btn-submit {
        background: linear-gradient(135deg, var(--primary), var(--primary-container));
        color: white;
        border: none;
        padding: 0.75rem 2rem;
        border-radius: 0.5rem;
        font-weight: 600;
        cursor: pointer;
        margin-right: 0.5rem;
    }

    .btn-cancel {
        background: #e6eeff;
        color: var(--primary);
        border: none;
        padding: 0.75rem 2rem;
        border-radius: 0.5rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
    }

    .info-box {
        background: var(--surface-container);
        border-radius: 0.75rem;
        padding: 1rem;
        margin-bottom: 1.5rem;
        border-left: 4px solid var(--primary);
    }
</style>

<div class="container-fluid py-4">
    <div class="mb-4">
        <h1 class="h3 mb-0" style="color: var(--primary); font-weight: 800; font-family: 'Manrope';">
            Chinh Sua Dat Phong @Model.MaDatPhong
        </h1>
        <p class="text-muted small mt-2">Cap nhat thong tin dat phong</p>
    </div>

    <div class="edit-container">
        <div class="info-box">
            <strong style="color: var(--primary);">Thong Tin Goc:</strong><br />
            Ma DP: <strong>@Model.MaDatPhong</strong> | Khach: <strong>@Model.MaKhachHangNavigation?.TenKhachHang</strong> | Ngay dat: <strong>@Model.NgayDat:dd/MM/yyyy</strong>
        </div>

        <form asp-action="Edit" method="post">
            <input type="hidden" name="MaDatPhong" value="@Model.MaDatPhong" />

            <div class="form-group">
                <label class="form-label">Trang Thai Dat Phong</label>
                <select class="form-select" name="TrangThai" required>
                    <option value="Chưa nhận phòng" selected="@(Model.TrangThai == "Chưa nhận phòng" || Model.TrangThai == "Chờ xác nhận" || Model.TrangThai == "Đã xác nhận")">Chưa nhận phòng</option>
                    <option value="Đã nhận phòng" selected="@(Model.TrangThai == "Đã nhận phòng" || Model.TrangThai == "Đã checkin")">Đã nhận phòng</option>
                    <option value="Đã hủy" selected="@(Model.TrangThai == "Đã hủy")">Đã hủy</option>
                    <option value="Hoàn thành" selected="@(Model.TrangThai == "Hoàn thành")">Hoàn thành</option>
                </select>
            </div>

            <div class="form-group">
                <label class="form-label">Ngay Nhan Phong (Du Kien)</label>
                <input type="date" class="form-control" name="NgayNhanDuKien" value="@Model.NgayNhanDuKien:yyyy-MM-dd" required />
            </div>

            <div class="form-group">
                <label class="form-label">Ngay Tra Phong (Du Kien)</label>
                <input type="date" class="form-control" name="NgayTraDuKien" value="@Model.NgayTraDuKien:yyyy-MM-dd" required />
            </div>

            <div class="form-group">
                <label class="form-label">Ma Khach Hang</label>
                <input type="text" class="form-control" name="MaKhachHang" value="@Model.MaKhachHang" readonly style="background: #f8f9ff; cursor: not-allowed;" />
                <small style="color: #757682; margin-top: 0.25rem; display: block;">De thay doi khach hang, vui long xoa va tao don moi</small>
            </div>

            <div style="display: flex; gap: 0.5rem; margin-top: 2rem;">
                <button type="submit" class="btn-submit">
                    <span class="material-icons" style="vertical-align: middle; margin-right: 0.5rem; font-size: 1.25rem;">save</span>
                    Luu Thay Doi
                </button>
                <a asp-action="Details" asp-route-id="@Model.MaDatPhong" class="btn-cancel" style="display: inline-flex; align-items: center;">
                    <span class="material-icons" style="margin-right: 0.5rem; font-size: 1.25rem;">close</span>
                    Huy
                </a>
            </div>
        </form>
    </div>
</div>
